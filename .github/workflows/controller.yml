name: Atlas Gardener controller

on:
  schedule:
    - cron: "15 10 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  attestations: read

concurrency:
  group: atlas-gardener-controller
  cancel-in-progress: false

jobs:
  controller:
    name: Validate and reconcile remediation
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      ATLAS_GARDENER_MODE: ${{ vars.ATLAS_GARDENER_MODE || 'disabled' }}
      ATLAS_GARDENER_WRITE_GATE: ${{ vars.ATLAS_GARDENER_WRITE_GATE || 'disabled' }}
      ATLAS_GARDENER_WRITE_TARGETS_JSON: ${{ vars.ATLAS_GARDENER_WRITE_TARGETS_JSON || '[]' }}
      AUTHORITY_SHA: eb634e5b19725ecc87902543058a4dd2a2e089c7
      BUNDLE_PATH: ${{ github.workspace }}/controller-input/gardener-findings.json
      EVIDENCE_PATH: ${{ github.workspace }}/controller-evidence/controller.json
      WORK_ROOT: ${{ github.workspace }}/controller-work
    steps:
      - name: Check out Atlas Gardener
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.0, Node 24

      - name: Check out reviewed Atlas Infra authority
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.0, Node 24
        with:
          repository: AtlasReaper311/atlas-infra
          ref: eb634e5b19725ecc87902543058a4dd2a2e089c7
          path: .atlas-infra
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v6.3.0, Node 24
        with:
          python-version: "3.12"

      - name: Validate source and reviewed authority
        env:
          ATLAS_GARDENER_CONTRACTS: ${{ github.workspace }}/.atlas-infra/contracts/v1
          ATLAS_GARDENER_INFRA_ROOT: ${{ github.workspace }}/.atlas-infra
        run: |
          set -euo pipefail
          python3 -m compileall -q src tests scripts
          PYTHONPATH=src python3 -m unittest \
            tests.test_automation \
            tests.test_automerge_gate \
            tests.test_automatic_github \
            tests.test_controller \
            tests.test_expanded_automation \
            tests.test_github_app_auth \
            tests.test_graph_fixers \
            tests.test_remediation_input \
            tests.test_security_fixers \
            tests.test_write_targets \
            -v
          python3 .atlas-infra/scripts/validate_gardener_automation.py --quiet
          python3 .atlas-infra/scripts/validate_gardener_target_readiness.py --quiet
          test "$(git -C .atlas-infra rev-parse HEAD)" = "$AUTHORITY_SHA"

      - name: Validate controller mode
        id: mode
        run: |
          set -euo pipefail
          case "$ATLAS_GARDENER_MODE" in
            disabled|observe|pr-only|automerge-low-risk)
              ;;
            *)
              printf 'Unknown ATLAS_GARDENER_MODE: %s\n' "$ATLAS_GARDENER_MODE" >&2
              exit 1
              ;;
          esac
          if [ "$ATLAS_GARDENER_MODE" = "pr-only" ] || [ "$ATLAS_GARDENER_MODE" = "automerge-low-risk" ]; then
            test "$ATLAS_GARDENER_WRITE_GATE" = "enabled"
          fi
          printf 'mode=%s\n' "$ATLAS_GARDENER_MODE" >> "$GITHUB_OUTPUT"

      - name: Restore the latest controller evidence for deduplication
        id: previous
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p controller-previous
          ARTIFACT_ID=$(gh api \
            "/repos/${GITHUB_REPOSITORY}/actions/artifacts?per_page=100" \
            --jq '[.artifacts[] | select(.expired == false and (.name | startswith("atlas-gardener-controller-")))] | sort_by(.created_at) | reverse | .[0].id // empty')
          if [ -z "$ARTIFACT_ID" ]; then
            printf 'path=%s\n' '' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          gh api "/repos/${GITHUB_REPOSITORY}/actions/artifacts/${ARTIFACT_ID}/zip" \
            > controller-previous.zip
          unzip -q controller-previous.zip -d controller-previous
          PREVIOUS=$(find controller-previous -type f -name controller.json -print -quit)
          if [ -z "$PREVIOUS" ]; then
            printf 'path=%s\n' '' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          python3 -m json.tool "$PREVIOUS" >/dev/null
          printf 'path=%s\n' "$PREVIOUS" >> "$GITHUB_OUTPUT"

      - name: Fetch the latest public Finding bundle
        if: steps.mode.outputs.mode != 'disabled'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p "$(dirname "$BUNDLE_PATH")"
          gh api \
            -H "Accept: application/vnd.github.raw+json" \
            "/repos/AtlasReaper311/atlas-dep-audit/contents/gardener-findings.json?ref=gardener-findings" \
            > "$BUNDLE_PATH"

      - name: Verify Finding bundle attestation
        id: attestation
        if: steps.mode.outputs.mode != 'disabled'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh attestation verify "$BUNDLE_PATH" \
            --repo AtlasReaper311/atlas-dep-audit
          printf 'verified=true\n' >> "$GITHUB_OUTPUT"

      - name: Run default-disabled controller
        if: steps.mode.outputs.mode == 'disabled'
        env:
          ATLAS_GARDENER_CONTRACTS: ${{ github.workspace }}/.atlas-infra/contracts/v1
          NOTIFY_TOKEN: ${{ secrets.NOTIFY_TOKEN }}
          PREVIOUS_EVIDENCE: ${{ steps.previous.outputs.path }}
        run: |
          set -euo pipefail
          mkdir -p "$(dirname "$EVIDENCE_PATH")"
          ARGS=(
            --infra-root .atlas-infra
            --output "$EVIDENCE_PATH"
            --work-root "$WORK_ROOT"
          )
          if [ -n "$PREVIOUS_EVIDENCE" ]; then
            ARGS+=(--previous-evidence "$PREVIOUS_EVIDENCE")
          fi
          PYTHONPATH=src python3 scripts/run_automatic_controller.py "${ARGS[@]}"

      - name: Run attested controller
        if: steps.mode.outputs.mode != 'disabled'
        env:
          ATLAS_GARDENER_APP_ID: ${{ vars.ATLAS_GARDENER_APP_ID }}
          ATLAS_GARDENER_APP_PRIVATE_KEY: ${{ secrets.ATLAS_GARDENER_APP_PRIVATE_KEY }}
          ATLAS_GARDENER_CONTRACTS: ${{ github.workspace }}/.atlas-infra/contracts/v1
          NOTIFY_TOKEN: ${{ secrets.NOTIFY_TOKEN }}
          PREVIOUS_EVIDENCE: ${{ steps.previous.outputs.path }}
        run: |
          set -euo pipefail
          mkdir -p "$(dirname "$EVIDENCE_PATH")"
          ARGS=(
            --infra-root .atlas-infra
            --bundle "$BUNDLE_PATH"
            --attestation-verified
            --output "$EVIDENCE_PATH"
            --work-root "$WORK_ROOT"
          )
          if [ -n "$PREVIOUS_EVIDENCE" ]; then
            ARGS+=(--previous-evidence "$PREVIOUS_EVIDENCE")
          fi
          PYTHONPATH=src python3 scripts/run_automatic_controller.py "${ARGS[@]}"

      - name: Upload bounded controller evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: atlas-gardener-controller-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ env.EVIDENCE_PATH }}
          if-no-files-found: error
          retention-days: 30
